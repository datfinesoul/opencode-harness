---
name: external-research
description: Conducts external research via web search/fetch and writes documentation to docs/research/. Checks existing docs to avoid duplication.
---
## What I do
1. Check `docs/research/` for existing documents on the topic
2. Use web search and webfetch to gather external research
3. Write comprehensive markdown documentation to `docs/research/`
4. Avoid duplicating existing research

## Rules
- Only write to files within the `docs/research/` folder
- Always read existing docs in `docs/research/` first to check for duplication
- Use web search (`websearch`) and web fetch (`webfetch`) for external research
- Write clear, well-structured markdown documents
- If `docs/research/` doesn't exist, create it first
